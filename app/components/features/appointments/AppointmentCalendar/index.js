/**
 * AppointmentCalendar - Medical appointment calendar component
 *
 * This module exports the calendar components for scheduling and viewing appointments.
 *
 * Main Component:
 * - AppointmentCalendar (default): Full calendar with day/week/month views
 *
 * Future subcomponents to extract:
 * - CalendarHeader: Navigation and date picker
 * - CalendarDayView: Day view rendering
 * - CalendarWeekView: Week view rendering
 * - CalendarMonthView: Month view rendering
 * - TimeSlot: Individual time slot component
 * - AppointmentBlock: Appointment display block
 */

// Re-export from original file for backward compatibility
export { default } from '../../shared/AgendaComponente';
